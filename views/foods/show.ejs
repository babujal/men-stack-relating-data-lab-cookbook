<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <title>
        <%= food.itemName %>
    </title>
</head>

<body>
    <%- include('../partials/_navbar.ejs') %>
        <h1>Item</h1>
        <p>
            <%= food.itemName %>
        </p>
        <p>
            <%= food.category %>
        </p>

        <form action="/users/<%= user._id %>/foods/<%= food._id %>?_method=DELETE" method="POST">
            <button type="submit">Delete this item</button>
        </form>

        <button>
            <a href="/users/<%= user._id %>/foods/<%= food._id %>/edit">Edit this application</a>
        </button>

        <button>
            <a href="/">Back</a>
        </button>

</body>

</html>